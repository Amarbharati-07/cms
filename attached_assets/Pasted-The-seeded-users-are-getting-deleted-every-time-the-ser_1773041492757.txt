The seeded users are getting deleted every time the server restarts.
I need all 21 users (1 admin + 20 candidates) to be PERMANENTLY stored 
in the database. They must NEVER be deleted on restart.

## ROOT CAUSE — FIX ALL OF THESE:

1. Remove any "deleteMany" or "drop table" or "sync({ force: true })" 
   from server startup code (server.js / index.js / app.js)
2. Remove any seed call from server.js startup
3. Make sure database uses persistent storage, NOT in-memory SQLite
4. Make sure .env DATABASE_URL points to a real persistent database file

---

## STEP 1 — USE PERSISTENT DATABASE

If using SQLite, change DATABASE_URL in .env to use a file path:
DATABASE_URL="file:./database.db"

If using PostgreSQL on Replit, make sure it is Replit's built-in 
PostgreSQL (not a temp instance). Go to Replit → Tools → Database 
and use the provided connection string.

---

## STEP 2 — FIX server.js / index.js

Make sure server startup looks like this — NO database reset on start:

// CORRECT - only connect, never drop or recreate
async function startServer() {
  await db.$connect();           // just connect
  // await runMigrations();      // optional: run pending migrations only
  app.listen(PORT, () => {
    console.log('Server running on port ' + PORT);
  });
}
startServer();

REMOVE any lines like:
- db.sync({ force: true })
- sequelize.sync({ force: true })
- sequelize.sync({ alter: true })
- mongoose.connection.dropDatabase()
- prisma.$executeRaw('DROP TABLE...')
- any seed() call inside startServer()

---

## STEP 3 — CREATE A SAFE SEED SCRIPT (runs only once)

Create file: seed.js

The seed script must use UPSERT logic so running it twice does NOT 
create duplicates and does NOT delete existing data:

const bcrypt = require('bcrypt');

async function seed() {
  console.log('Starting permanent seed...');

  const users = [
    { email: 'admin@fieldtrack.com',     password: 'Admin@123', role: 'ADMIN', name: 'Admin', state: null },
    { email: 'candidate1@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Rahul Sharma',   state: 'Rajasthan',      phone: '9810001001', age: 24 },
    { email: 'candidate2@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Priya Verma',    state: 'Maharashtra',    phone: '9810001002', age: 23 },
    { email: 'candidate3@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Amit Singh',     state: 'Gujarat',        phone: '9810001003', age: 25 },
    { email: 'candidate4@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Neha Patel',     state: 'Uttar Pradesh',  phone: '9810001004', age: 22 },
    { email: 'candidate5@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Ravi Kumar',     state: 'Madhya Pradesh', phone: '9810001005', age: 26 },
    { email: 'candidate6@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Sunita Devi',    state: 'Bihar',          phone: '9810001006', age: 24 },
    { email: 'candidate7@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Vikram Yadav',   state: 'Rajasthan',      phone: '9810001007', age: 27 },
    { email: 'candidate8@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Kavita Joshi',   state: 'Maharashtra',    phone: '9810001008', age: 23 },
    { email: 'candidate9@fieldtrack.com',  password: 'Pass@1234', role: 'CANDIDATE', name: 'Deepak Mishra',  state: 'Gujarat',        phone: '9810001009', age: 25 },
    { email: 'candidate10@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Pooja Tiwari',   state: 'Uttar Pradesh',  phone: '9810001010', age: 22 },
    { email: 'candidate11@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Suresh Rawat',   state: 'Madhya Pradesh', phone: '9810001011', age: 28 },
    { email: 'candidate12@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Anita Chauhan',  state: 'Bihar',          phone: '9810001012', age: 24 },
    { email: 'candidate13@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Manoj Gupta',    state: 'Rajasthan',      phone: '9810001013', age: 26 },
    { email: 'candidate14@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Rekha Nair',     state: 'Maharashtra',    phone: '9810001014', age: 23 },
    { email: 'candidate15@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Ajay Pandey',    state: 'Gujarat',        phone: '9810001015', age: 25 },
    { email: 'candidate16@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Meena Kumari',   state: 'Uttar Pradesh',  phone: '9810001016', age: 22 },
    { email: 'candidate17@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Rohit Bhatt',    state: 'Madhya Pradesh', phone: '9810001017', age: 27 },
    { email: 'candidate18@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Shalini Singh',  state: 'Bihar',          phone: '9810001018', age: 24 },
    { email: 'candidate19@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Dinesh Agarwal', state: 'Rajasthan',      phone: '9810001019', age: 26 },
    { email: 'candidate20@fieldtrack.com', password: 'Pass@1234', role: 'CANDIDATE', name: 'Preeti Saxena',  state: 'Maharashtra',    phone: '9810001020', age: 23 },
  ];

  for (const u of users) {
    const hashed = await bcrypt.hash(u.password, 10);

    // UPSERT — create if not exists, skip if already exists
    const existing = await db.user.findUnique({ where: { email: u.email } });
    if (existing) {
      console.log('Already exists, skipping: ' + u.email);
      continue;
    }

    const created = await db.user.create({
      data: { email: u.email, password: hashed, role: u.role }
    });

    if (u.role === 'CANDIDATE') {
      await db.candidateProfile.create({
        data: {
          userId: created.id,
          fullName: u.name,
          state: u.state,
          phone: u.phone,
          age: u.age
        }
      });
    }
    console.log('Created: ' + u.email);
  }

  const total = await db.user.count();
  console.log('Total users in database: ' + total);
  console.log('SEED COMPLETE — data is permanently saved.');
}

seed().catch(console.error);

---

## STEP 4 — ADD TO package.json

"scripts": {
  "dev": "node server.js",
  "seed": "node seed.js",
  "start": "node server.js"
}

---

## STEP 5 — VERIFY PERMANENCE

After seeding, restart the server and run this check:

// Add this temporary route to verify data persists after restart
app.get('/api/check-users', async (req, res) => {
  const count = await db.user.count();
  const users = await db.user.findMany({ 
    select: { email: true, role: true } 
  });
  res.json({ total: count, users });
});

Visit: yourapp.replit.dev/api/check-users
After server restart it must still show 21 users.
Remove this route after verification.

---

## STEP 6 — REPLIT SPECIFIC

If using Replit with SQLite:
- Make sure database.db file is in the root project folder
- Add database.db to version control (do NOT add to .gitignore)
- In replit.nix or .replit config, make sure the db file path is writable

If using Replit PostgreSQL:
- Go to Tools → PostgreSQL in Replit sidebar
- Copy the DATABASE_URL and paste in .env
- PostgreSQL on Replit is always persistent

---

## FINAL CHECK AFTER EVERYTHING:

1. Run: npm run seed
2. Restart server: stop and start again
3. Visit: /api/check-users → must show 21 users
4. Login with admin@fieldtrack.com / Admin@123 → must work
5. Login with candidate1@fieldtrack.com / Pass@1234 → must work
6. Restart server AGAIN
7. Login again → must still work

Only if all 7 steps pass = data is truly permanent.