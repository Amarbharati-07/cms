The Attendance, Tasks, and Progress pages currently show "No records found" empty states. 
I need all three pages to display proper data tables with the following exact requirements:

---

## 1. ATTENDANCE PAGE (/admin/attendance)

Replace the empty state with a full table. Add date range filter + state filter on top.

Table columns:
| # | Candidate Name | State | Date | Check-in Time | Photo | GPS Location | Status |

- Show one row per attendance entry per day per candidate
- "Photo" column: show a small thumbnail (click to enlarge in modal)
- "GPS Location": show as "lat, long" with a small map icon that opens Google Maps link
- "Status" column: show badge — Present (green) / Absent (red) / Late (yellow)
- Add a date filter (date picker) and state dropdown filter at the top
- Show total count: "Showing X attendance records"
- If no records, show empty state with message

---

## 2. TASKS PAGE (/admin/tasks)

Replace or enhance the task list with a full detailed table view.

Table columns:
| # | Task Title | Assigned To | State | Deadline | Status | Submitted On | Proof | Action |

- "Status" badge colors: Pending=yellow, Submitted=blue, Approved=green, Rejected=red
- "Proof" column: show thumbnail if photo submitted, video icon if video — click to open modal
  Modal shows: full image/video + GPS coordinates + timestamp + SHA-256 hash
- "Action" column (admin only): Approve button + Reject button (only if status is Submitted)
- Add filters: Status filter dropdown + State filter dropdown + Search by candidate name
- Show task completion summary bar on top: e.g. "12/20 tasks completed (60%)"

---

## 3. PROGRESS PAGE (/admin/progress or inside dashboard)

Create a full progress tracking table for all 20 candidates.

Table columns:
| # | Candidate Name | State | Tasks Assigned | Tasks Completed | Completion % | Attendance % | Last Active | Profile Complete |

- "Completion %" column: show a colored progress bar inside the cell
  - 0-40% = red bar
  - 41-70% = yellow bar  
  - 71-100% = green bar
- "Attendance %" column: same colored progress bar
- "Profile Complete" column: green checkmark if 100%, orange warning if incomplete
- "Last Active" column: show relative time e.g. "2 hours ago", "3 days ago"
- Click on any candidate row → opens candidate detail page
- Add state filter dropdown at top
- Show summary cards above table:
  - Total Candidates: 20
  - Avg Task Completion: X%
  - Avg Attendance: X%
  - States Covered: 6

---

## 4. DAILY ATTENDANCE TRACKING (for candidate side)

On the candidate's attendance page:

Table columns:
| Date | Day | Check-in Time | Location | Photo | Status |

- Show every day of the current month
- Days not yet marked: show "Not Marked" in grey
- Days marked: show time + location + photo thumbnail
- Show monthly summary: "Present: X days | Absent: X days | Attendance Rate: X%"

---

## 5. GENERAL TABLE REQUIREMENTS (apply to all tables)

- Use a dark-themed table style matching the existing FieldTrack dark UI
- Add pagination: show 10 rows per page with Previous/Next buttons
- Add CSV export button on every table (downloads current filtered data)
- Column headers should be sortable (click to sort ascending/descending)
- Tables must be responsive (horizontal scroll on mobile)
- Use the same color scheme: dark background #0f172a, blue accent #3b82f6

---

## 6. SEED/MOCK DATA

Since the database may be empty, add mock data seed so tables are not empty during development:
- Seed 20 candidates with profiles across 6 states
- Seed attendance for last 30 days for each candidate (random present/absent)
- Seed 3-5 tasks per candidate with mixed statuses (pending/submitted/approved/rejected)
- Seed some submissions with placeholder image URLs

Run seed with: npm run seed
or add a "Load Sample Data" button in the admin dashboard for testing.