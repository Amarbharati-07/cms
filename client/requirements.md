## Packages
date-fns | Date formatting and manipulation

## Notes
- Using standard HTML5 Geolocation API for location tagging
- Using standard <input type="file"> for camera access and file uploads
- Custom fetch functions used for FormData to avoid default application/json headers
- Dark corporate aesthetic enforced via root CSS variables
